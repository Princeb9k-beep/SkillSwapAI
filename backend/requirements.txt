# SkillSwap AI — backend dependencies
# NOTE: `superpowers`, `ecc`, and `system-design-primer` are intentionally NOT here.
# They are Claude Code skills / study docs (vendored under ../claude_skills/), not
# PyPI packages — installing them by name would fail or pull unrelated projects.
# Their *patterns* are implemented in real code (see app/concurrency.py,
# app/resilience.py, app/responses.py, app/skills/). See claude_skills/INTEGRATION.md.

fastapi==0.115.6
uvicorn[standard]==0.34.0
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0
psycopg2-binary==2.9.10        # sync driver for Alembic offline/utility use
alembic==1.14.0
redis==5.2.1
pydantic==2.10.4
pydantic[email]==2.10.4       # EmailStr validation
email-validator==2.2.0
pydantic-settings==2.7.1
python-dotenv==1.0.1
groq==0.15.0
httpx==0.28.1
werkzeug==3.1.3               # password hashing helpers (auth scaffolding)
jinja2==3.1.5                # resume template rendering
aiosqlite==0.20.0            # async sqlite driver used for local tests/verification
