# SkillSwap AI — environment configuration
# Copy to `.env` and fill in the real values. `.env` is git-ignored.

# PostgreSQL
POSTGRES_HOST=dpg-d9d9ufmrnols73f3hkag-a
POSTGRES_PORT=5432
POSTGRES_DB=skillswapaidb
POSTGRES_USER=prince
POSTGRES_PASSWORD=${POSTGRES_PASSWORD}

# Optional: a full async URL overrides the discrete POSTGRES_* vars above.
# Render's "Internal Database URL" is postgres:// — the app rewrites it to
# postgresql+asyncpg:// automatically. Leave blank to build from the parts above.
DATABASE_URL=

# Redis
REDIS_URL=redis://red-d9d9vt37uimc73aiotig:6379
REDIS_PASSWORD=${REDIS_PASSWORD}

# Groq AI
GROQ_API_KEY=${GROQ_API_KEY}
GROQ_MODEL=llama-3.3-70b-versatile

# App Config
APP_ENV=production
APP_SECRET_KEY=${APP_SECRET_KEY}
APP_DEBUG=False

# Comma-separated list of allowed CORS origins for the frontend (or * for all)
CORS_ORIGINS=*
